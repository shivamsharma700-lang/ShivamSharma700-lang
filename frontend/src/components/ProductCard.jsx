import { motion } from 'framer-motion'; import { Heart } from 'lucide-react'; import { Link } from 'react-router-dom';
export default function ProductCard({product}){return <motion.article whileHover={{y:-8}} className="group overflow-hidden rounded-[2rem] border border-white/10 bg-white/[.04]"><Link to={`/products/${product.slug}`}><div className="relative aspect-[3/4] overflow-hidden bg-neutral-900"><img className="h-full w-full object-cover transition duration-700 group-hover:scale-110" src={product.imageUrls?.[0]||`https://images.unsplash.com/photo-1516257984-b1b4d707412e?q=80&w=900&auto=format&fit=crop`} alt={product.name}/><button className="absolute right-4 top-4 rounded-full bg-black/50 p-3 backdrop-blur"><Heart size={18}/></button></div><div className="p-5"><p className="text-xs uppercase tracking-[.28em] text-champagne">Vocal Atelier</p><h3 className="mt-2 font-semibold">{product.name}</h3><div className="mt-3 flex items-center gap-3"><b>₹{product.price}</b>{product.compareAtPrice&&<span className="text-sm text-white/40 line-through">₹{product.compareAtPrice}</span>}</div></div></Link></motion.article>}
