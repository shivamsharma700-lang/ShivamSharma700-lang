import { useState } from 'react'; import { api } from '../lib/api'; import { useDispatch } from 'react-redux'; import { setAuth } from '../app/store';
export default function Auth(){const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const dispatch=useDispatch(); async function submit(e){e.preventDefault(); const {data}=await api.post('/auth/login',{email,password}); dispatch(setAuth(data));} return <main className="container-x grid min-h-[70vh] place-items-center py-16"><form onSubmit={submit} className="glass w-full max-w-md rounded-[2rem] p-8"><h1 className="font-display text-5xl">Welcome back</h1><input className="mt-8 w-full rounded-full bg-white/10 px-5 py-3" placeholder="Email" onChange={e=>setEmail(e.target.value)}/><input className="mt-4 w-full rounded-full bg-white/10 px-5 py-3" type="password" placeholder="Password" onChange={e=>setPassword(e.target.value)}/><button className="btn-primary mt-6 w-full">Login</button><button type="button" className="btn-ghost mt-3 w-full">Continue with Google</button></form></main>}
